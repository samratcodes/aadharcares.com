/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.pexels.com', 'images.pexels.com','res.cloudinary.com','via.placeholder.com','images.unsplash.com', 'i.pinimg.com']
    },
};

export default nextConfig;
